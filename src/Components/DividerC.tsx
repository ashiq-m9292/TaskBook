import React from 'react';
import { Divider } from 'react-native-paper';

const DividerC = ({ bold, style }: any) => {
    return (
        <Divider
            bold={bold}
            style={style}
        />
    );
}

export default DividerC;
